type: "object"
properties:
  mediaType:
    type: "string"
    enum:
      - "film"
      - "series"
    description: "The media type of this entry."
  parentName:
    type: "string"
    description: "The name of the parent."
  monitored:
    type: "boolean"
    description: "Indicates whether the entry is monitored."
  mediaFilePath:
    type: "string"
    description: "The file path the media entry is located at."
